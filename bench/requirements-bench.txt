# Dependências do benchmark de parsers de documentos (PDF -> Markdown).
# Instale em um venv isolado (NÃO no ambiente da API/pipeline).
#
# ATENÇÃO pillow: marker-pdf/surya exigem pillow<11; mineru exige pillow>=11.
# Docling + Marker convivem no mesmo venv (pillow<11). Para testar MinerU, use
# um venv SEPARADO.
#
# Marker exige ainda o binário nativo `llama-server` (llama.cpp) no PATH
# (ou LLAMA_CPP_BINARY). Veja o README.

docling
marker-pdf
pillow<11,>=10.2
reportlab        # só para gerar o fixture sintético (gen_pdf.py)
pypdfium2        # sondagem de páginas/camada de texto (já vem com docling)
